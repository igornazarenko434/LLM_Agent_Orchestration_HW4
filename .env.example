# ============================================================================
# Environment Variables Configuration for hw4_tourguide
# ============================================================================
#
# This file documents all environment variables used by the Route Enrichment
# Tour-Guide System. Copy this file to .env and fill in your actual API keys.
#
# IMPORTANT: Never commit the .env file to version control!
# The .env file is listed in .gitignore to prevent accidental commits.
#
# ============================================================================

# ----------------------------------------------------------------------------
# Google Maps API (REQUIRED for live mode)
# ----------------------------------------------------------------------------
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Enable: Directions API, Geocoding API
# Free tier: 2,500 requests/month
# Cost: $0.005 per request (after free tier)
#
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ----------------------------------------------------------------------------
# YouTube Data API v3 (OPTIONAL - for Video Agent)
# ----------------------------------------------------------------------------
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Enable: YouTube Data API v3
# Free tier: 10,000 quota units/day (100 search requests)
# Cost: Free for typical usage
#
YOUTUBE_API_KEY=your_youtube_api_key_here

# ----------------------------------------------------------------------------
# Spotify Web API (OPTIONAL - for Song Agent)
# ----------------------------------------------------------------------------
# Get credentials from: https://developer.spotify.com/dashboard
# You'll need both Client ID and Client Secret
# Free tier: Unlimited for non-commercial use
#
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# ----------------------------------------------------------------------------
# Wikipedia / DuckDuckGo (Knowledge Agent)
# ----------------------------------------------------------------------------
# These APIs are keyless for typical usage; keep entries for clarity. Leave
# blank unless a future provider requires a key.
#
WIKIPEDIA_API_KEY=
DUCKDUCKGO_API_KEY=

# ----------------------------------------------------------------------------
# OpenAI API (OPTIONAL - for Judge LLM scoring)
# ----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# Used for: Judge agent LLM-assisted scoring (if enabled)
# Cost: ~$0.002 per 1K tokens (GPT-3.5-turbo)
#
OPENAI_API_KEY=your_openai_api_key_here

# ----------------------------------------------------------------------------
# Anthropic Claude API (OPTIONAL - for Judge LLM scoring alternative)
# ----------------------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/
# Used for: Judge agent LLM-assisted scoring (if enabled)
# Cost: Varies by model
#
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ----------------------------------------------------------------------------
# Google Gemini API (OPTIONAL - for Judge LLM scoring alternative)
# ----------------------------------------------------------------------------
# Get your API key from: https://aistudio.google.com/app/apikey
# Used for: Judge agent LLM-assisted scoring (if enabled)
# Cost: Free tier available; model-dependent
#
GEMINI_API_KEY=your_gemini_api_key_here

# ----------------------------------------------------------------------------
# Application Settings (OPTIONAL - override config/settings.yaml)
# ----------------------------------------------------------------------------
# You can override configuration values here, though it's recommended
# to use config/settings.yaml for most settings and CLI flags for one-off changes.
#
# Example overrides:
# LOG_LEVEL=DEBUG
# SCHEDULER_INTERVAL=2.0
# MAX_WORKERS=5

# ----------------------------------------------------------------------------
# Development Settings (OPTIONAL)
# ----------------------------------------------------------------------------
# Enable debug mode (more verbose logging, no timeouts)
# DEBUG=false

# Mock mode for testing (uses mock API responses instead of real APIs)
# MOCK_MODE=false
